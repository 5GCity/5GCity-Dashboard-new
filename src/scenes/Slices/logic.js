/**
 * Slices Container Logic
 * Please write a description
 *
 * @author Joana Garrido <jgarrido@ubiwhere.com>
 */

import { kea } from 'kea'

export default kea({
  path: () => ['scenes', 'Slices'],

  actions: () => ({
    
  }),

  reducers: ({ actions }) => ({
    

  }),

  start: function * () {
  },

  takeLatest: ({ actions, workers }) => ({
    
  }),


  workers: {
    
  }

})

