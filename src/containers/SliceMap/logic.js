/**
 * SliceMap Container Logic
 * Please write a description
 *
 * @author Your Name <gpatriarca@ubiwhere.com>
 */

import { kea } from 'kea'

export default kea({
  path: () => ['scenes', 'containers', 'SliceMap'],

  actions: () => ({
  }),

  reducers: ({ actions }) => ({
  }),

  takeLatest: ({ actions, workers }) => ({
  }),

  workers: {
  }

})

